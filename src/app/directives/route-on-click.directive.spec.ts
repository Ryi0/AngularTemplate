import { RouteOnClickDirective } from './route-on-click.directive';

describe('RouteOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
